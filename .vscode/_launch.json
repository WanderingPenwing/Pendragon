{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run selected file",
            "type": "node",
            "request": "launch",
            "program": "/usr/local/cargo/bin/pendragon",
            "args": ["${file}"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
